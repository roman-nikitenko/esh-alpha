import React from 'react';
import './header.scss'
import { Navigation } from "./Navigation";

export const Header = () => {
  return (
    <section className="Header">
      <Navigation />
    </section>
  );
};
